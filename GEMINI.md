# Gemini CLI 저장소 지침

이 파일은 도구용 어댑터다. 정책의 단일 기준은 루트 `AGENTS.md`다.

- 작업 시작 전 루트와 수정 대상에 가장 가까운 `AGENTS.md`를 읽고 따른다.
- 커밋·PR·문서·주석·결과 보고는 한글을 기본으로 한다.
- 사용자 변경과 다른 worktree를 보존하고 요청 범위 밖의 리팩터링을 섞지 않는다.
- 검증을 우회하거나 실패를 숨기지 않는다.
- 완료 전 `pnpm harness:verify`와 관련 영역 테스트를 실행한다.
- 배포는 별도 명시적 승인 없이는 수행하지 않는다.

세부 규칙은 복제하지 않으며 충돌 시 `AGENTS.md`가 우선한다.
