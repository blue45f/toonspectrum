# 드로잉 레이아웃 감사 후속 구현 · 2026-09-22

상태: **PR #1946 main 병합 완료 · post-merge 관련 테스트 회귀 후속 수정 및 재검증**. 운영 배포 상태와는 별개이며, 이 문서만으로 운영 반영을 뜻하지 않는다.

## 1. 확인한 문제와 구현 범위

운영 감사에서 확인한 주요 원인은 문서 셸과 편집기가 서로의 점유 공간을 모른 채 fixed/absolute UI를 올리는 구조였다. 이번 변경은 문서·브러시·저장·협업 권위를 바꾸지 않고, 실제 편집기의 공간 배정과 패널 정보 우선순위를 수정한다.

| 항목 | 기존 문제 | 구현 |
|---|---|---|
| L01 | 좌하단 모드 배지가 구성·선택·펜을 가림 | 모드 컨트롤을 실제 문서 명령 행의 포털 슬롯으로 이동 |
| L02 | 작업공간 선택 바가 굵기·불투명도·주 색을 가림 | 기본 위치를 기존 명령 행에 통합; 실제 메뉴 높이를 측정해 모바일 상단 인셋에 공유 |
| L03 | 상태 바가 캔버스 및 도움말 영역을 넘음 | 캔버스 컨테이너 기준 너비 제한, 도움말 공간 예약, 가로 스크롤 유지, 오버레이 브러시 너비 회피 |
| L04 | 브러시 목록보다 설명·설정이 공간을 차지 | 교체·너비 설정 접기, 중복 설명 압축, 결과 목록 우선 |
| L05 | 레이어 시트의 첫 화면에 실제 레이어가 안 보임 | 긴 선택 요약·현재 도구 안내는 제거하되 44px 설정 복귀 버튼은 헤더에 유지, 기본 목록 시트 높이 확보, 낮은 높이에서 일반 도움말 압축 |
| L06 | 1280px 전환에서 캔버스 공간이 역전 | 임의의 1280px 컷오프 대신 레일·인스펙터·분할선·브러시를 제외한 실제 잔여 폭으로 도킹 판단 |
| L07 | 작은 구성 설정 창의 실제 목록이 아래로 밀림 | 목록을 보기 방식·일괄 편집보다 먼저 배치; 저장 완료 상태의 중복 하단 바 제거 |
| L08 | 시트 뒤의 일부 셸·저장 UI가 위로 남음 | 기본 셸 컨트롤을 편집기 모달 경계 안으로 통합; 저장·오프라인 배지는 스크림 뒤에 유지 |

1024px의 기존 모바일/데스크톱 전환 자체를 없앤 변경은 아니다. 태블릿 전용 도구 체계를 새로 만들거나 사용자가 명시적으로 배치한 모든 커스텀 창을 자동 재배치한 것도 아니다.

## 2. 구조와 데이터 보존

`StudioDocumentChromeSlot`이 문서 메뉴 안에 DOM 슬롯을 제공하고, 셸의 작업공간 컨트롤과 모드 컨트롤이 그 슬롯에 포털로 렌더링된다. 모드·문서 상태의 React 소유자는 유지한다. 슬롯이 늦게 생기거나 사라지는 경우를 테스트하며, 메뉴의 실제 높이 측정값은 종료 시 이전 값으로 복원한다.

`StudioShellFloatingTarget`은 기본 배치일 때만 슬롯의 authored 위치를 따른다. 명시적으로 저장한 커스텀 배치와 배치 편집 동작은 기존 경로를 유지한다. 경고·저장 메시지를 삭제하거나 숨김 설정으로 안전 상태를 무시하지 않는다.

브러시 도킹은 현재 도구 레일의 실제 오른쪽 좌표를 사용한다. 페이지 레일까지 포함하므로 1024px 부근에서 브러시가 도구 레일 자체를 덮던 좌표 오류도 함께 고친다. 포인터 스트로크·IME 조합 중에는 기존 재배치 지연 규칙을 유지한다.

## 3. 브라우저 검증

`node scripts/verify-studio-drawing-layout.mjs`는 로컬 origin에서 별도 일러스트를 생성한다. 인증 세션은 비로그인 fixture로, 나머지 API는 503으로 처리해 실제 계정·작품·유료 서비스에 접근하지 않고 오프라인 안전 메시지도 함께 검증한다.

### 기본 화면 20개

1139×768, 1140×768, 1279×720, 1440×900, 1920×1080, 2560×1440, 1366×768, 1280×720, 1180×820, 1024×768, 1023×768, 820×1180, 768×1024, 430×932, 390×844, 375×667, 360×800, 320×568, 844×390, 667×375를 확인한다. 모두 CSS 픽셀이다.

각 크기에서 문서 작업공간·모드·구성 또는 모바일 선택·펜 버튼의 실제 DOM 존재와 중심 hit-test, 문서 가로 넘침, 캔버스 존재, 상태 바의 캔버스 경계 준수를 검사한다. 빈 선택자 결과를 통과로 처리하지 않는다. 비활성 상태도 별도로 기록한다.

### 실제 조작 6개 묶음

1. 320×568에서 첫 구성 행이 스크롤 없이 보이고 취소가 동작한다.
2. 전체 35개 도구를 두 열로 적용하고 마지막 도구·구성 버튼까지 도달한다.
3. 같은 35개를 이름 목록으로 적용하고 마지막 도구·구성 버튼까지 도달한다.
4. 같은 35개를 한 열로 적용하고 마지막 도구·구성 버튼까지 도달한다.
5. 선택→펜 전환 뒤 Chromium 터치 입력으로 실제 획을 생성한다. 레이어 행과 44px 설정 복귀 버튼이 320×568·390×844·844×390·667×375·820×1180의 시트 첫 화면에 함께 보이고 입력을 받으며, 설정 복귀 후 선택 항목 탭에 포커스가 이동한다.
6. 주 색 편집의 적용·취소가 390×844·375×667·844×390에서 조작 가능하고, 취소 후 캔버스로 돌아온다.

`trial: true`는 강제 클릭이 아니라 Playwright의 표시·안정성·입력 수신·활성 상태 검사를 수행한다. 도구 구성 적용·취소, 펜 전환, 레이어 열기와 터치 획은 실제 동작시킨다. 레이어 표시 상태와 색상은 검증 편의를 위해 실제 변경하지 않는다.

### 구현 후 실측 예

| 화면 | 브러시 결과 영역 높이 | 캔버스 뷰포트 | 비고 |
|---|---:|---:|---|
| 1140×768 | 193px | 480×563px | 분할선까지 포함한 최소 도킹 경계 |
| 1280×720 | 145px | 620×515px | 하단 상태 바 너비 444px, 우측 보조 컨트롤 공간 예약 |
| 1366×768 | 193px | 706×563px | 목록 공간 확보 |
| 1440×900 | 325px | 780×695px | 기존 메뉴 높이 90px 유지, 별도 플로팅 바 제거 |
| 844×390 | 해당 없음 | 전체 화면 위 오버레이 | 문서 메뉴·작업공간을 54px 높이의 한 행으로 배치 |

캔버스 뷰포트는 DOM상의 영역이다. 위에 올라온 도구를 뺀 순수 작화 면적이나 기기 물리 해상도와 같지 않다. 이전 운영 감사와 현재 main 기반 로컬 빌드의 SHA가 다르므로 모든 수치를 동일 빌드 A/B 성능 비교로 읽으면 안 된다.

## 4. 검증 기록과 재현

- PR 전 관련 단위·컴포넌트·구조 테스트: 12개 파일, 97개 테스트 통과.
- post-merge 후속에서는 `StudioInspectorNavigator` 16개 테스트를 통과시켰고, 원 PR의 19개 변경 소스를 입력으로 실패했던 동일 related 테스트 게이트를 다시 실행해 78개 파일·740개 테스트가 모두 통과했다.
- 변경된 TypeScript/JavaScript 파일의 strict ESLint 통과.
- 웹 TypeScript 검사 통과. 최초 기본 힙 실행은 메모리 제한으로 종료되어 저장소의 12GB 힙 설정으로 다시 검사했다.
- Chromium 개발 서버 및 로컬 운영용 번들 미리보기에서 각각 20개 화면과 6개 조작 묶음 통과. PR 생성 전 운영용 번들에서 같은 20개 화면·6개 조작 묶음을 재실행해 통과했다. 운영 사이트 배포 또는 실물 기기 인증을 뜻하지 않는다.
- 포털로 옮긴 제작 모드 메뉴도 1440×900·390×844에서 실제로 열고 내부 내보내기 버튼의 입력 수신을 확인했다. 검증 중 실제 내보내기·게시를 실행하지 않았다.
- 아키텍처 경계 검사 통과. 전체 저장소 테스트 실행이나 원격 PR CI 통과와 구분한다.

```sh
# 저장소 루트에서 로컬 서버 또는 빌드 미리보기 실행 후:
TOONSPECTRUM_VERIFY_ORIGIN=http://127.0.0.1:5427 \
TOONSPECTRUM_VERIFY_DIR=/tmp/toonstudio-layout-remediation-20260922 \
node scripts/verify-studio-drawing-layout.mjs
```

결과는 `layout-verification.json`, `after-*.png`, `interaction-*.png`에 기록한다. 원본 스크린샷과 측정 JSON은 Git 소스에 커밋하지 않는다. API 실패 fixture에서 나온 서버 장애 메시지는 레이아웃 회귀와 구분한다.

## 5. 남은 검증·설계 범위

실물 iPhone/iPad Safari, Android Chrome, 주소창 접힘·노치·홈 인디케이터, 실제 소프트 키보드와 IME, 실물 펜의 필압·팜 리젝션은 확인하지 않았다. 기존 웹뷰·브라우저별 이슈까지 해결됐다고 주장하지 않는다.

태블릿 전용 작업 레일과 1024px 전환 구조의 통합, 사용자가 임의로 넓힌 여러 패널의 모든 조합, 저장된 자유 배치의 자동 충돌 회피는 별도 설계 범위다. 이번 패치에서는 기존 입력/도구 권위와 사용자 설정을 보존하고, 확인한 기본 배치의 가림과 목록 접근성을 먼저 고쳤다.

PR #1946은 main에 병합됐다. 이 post-merge 후속 변경의 PR·main 병합 상태는 Git 기록으로 별도 확인하며, 운영 배포는 별도의 사용자 승인 없이 수행하지 않는다.

## 6. PR #1946 병합 정본과 증거

- PR #1946 head: `72cc98b907584c425a9c8cb2001e5f71335023db`.
- main 병합 커밋: `6b3fce611ec7a1e420c96bbd1e887fdb4b9c8d4c`.
- 후속 작업 초기 기준 main: `86e2ca91d18376b7512dcfbf5f14369644b50b86`.
- PR #1957 병합 후 최종 검증 기준 main: `d77f5d8cbd39ebc01649af380994e526fba44c1c`.
- 원 PR 직전 재검증: `/tmp/toonstudio-layout-remediation-20260922/pr-final/layout-verification.json`.
- 원 PR 로컬 운영용 번들 증거: `/tmp/toonstudio-layout-remediation-20260922/production-preview/`.
- 제작 모드 메뉴 추가 확인: `/tmp/toonstudio-layout-remediation-20260922/pr-shell-review/`.
- post-merge 개발 서버 재검증: `/tmp/toonstudio-layout-postmerge-final-20260923/`.
- post-merge 운영용 번들 재검증: `/tmp/toonstudio-layout-postmerge-production-20260923/`.
- 브라우저 증거와 원시 측정값은 Git 소스에 넣지 않는다.

## 7. post-merge 관련 테스트 회귀 폐쇄

PR #1946이 main에 병합된 뒤 `ToonStudio session goals`의 related 테스트가 완료됐고,
`StudioInspectorNavigator.test.tsx` 16개 중 2개가 실패했다. 실패한 계약은 레이어 탭에서 선택
항목 설정으로 바로 돌아가는 버튼과, 버튼 실행 후 선택 항목 탭으로 포커스를 옮기는 동작이었다.
목록 공간을 우선하면서 `layout.primary === "layers"`에서 전체 CTA 행을 제거한 조건이 직접
복귀 경로까지 함께 없앤 것이 원인이었다.

후속 수정은 큰 안내 행을 되살리지 않는다. 레이어 탭의 헤더에 44×44px의 compact 설정 복귀
버튼을 두고, 선택 이름·설명·`설정 열기` 문구는 접근 가능한 이름과 스크린리더 텍스트로
유지한다. 따라서 낮은 높이에서도 레이어 행의 첫 화면 우선순위를 보존하면서, 포인터·터치·
키보드 사용자는 선택 항목 설정으로 바로 이동하고 이동한 탭에 포커스를 이어받는다.

검증 스크립트도 레이어 행만 확인하지 않고 다섯 가지 화면 크기에서 compact 설정 복귀 버튼의
가시성과 입력 수신을 함께 검사한다. 마지막에는 버튼을 실제 실행해 선택 항목 탭의
`aria-selected=true`와 DOM 포커스를 확인한다. 현재 main 기반 Chromium 개발 서버와 새로 만든 운영용 번들 미리보기에서 각각 20개
화면 크기와 6개 조작 묶음을 다시 실행해 모두 통과했으며, 처리되지 않은 페이지 오류는 없었다.
운영용 번들은 TypeScript 검사·Vite 빌드·third-party notices 생성·정적 CSP 검증까지 통과했다.
이 후속은 다른 세션의 파일을 수정하지 않는 독립 worktree에서 진행하며, 현재 열려 있는 다른
PR의 변경 경로와 겹치지 않는지 PR 생성과
main 병합 직전에 다시 확인한다.

운영 배포는 이 main 병합 작업에 포함하지 않는다.
