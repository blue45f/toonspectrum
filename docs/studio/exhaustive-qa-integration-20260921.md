# Consolidated Studio QA recovery — 2026-09-21

This integrates immutable repair commits `39ab110bdf9ba951f1e9e60da3b040b627cd389c` and `61b84ba0e2c7e429da7c05b6c09cded02136728c` without editing their original worktrees. Previous checkpoint documents remain historical, not final delivery status.

## Integrated behavior

- Preserve passive lifecycle recovery observation and all original document identity, Undo/Redo, PNG/SVG export and pixel equality checks. Automatic recovery must have been observed; a missing/blocked/manual notice cannot become an invented success.
- Resolve the actual production GLTFLoader module through its complete public method contract, without assuming a minified alias or constructing candidates. Retain imported-module ambiguity/missing/error tests from the isolated repair.
- Enter the actual professional 3D workspace and visible imported-layer dock before asserting diagnostic/import behavior. Keep original native GPU, worker, raster, texture, KTX2 and cleanup assertions.
- Record only the exact static-preview loopback readiness502 as unavailable, never as successful Core API readiness. Other errors remain failures.
- Mobile launch opens the native “세부 선 보정 설정” disclosure before accessing its controls, then uses the canonical current-brush editor label. Parent controls are checked before/after the nested inert modal, not while inaccessible.

## Verification and remaining blocker

- Integrated focused verifier suite:5 files/115 tests passed.
- Canonical lifecycle passed against unchanged production dist; the complete PNG pre/post-reload equality and real automatic-recovery path remain required.
- Full original 3D gate passed again on the integrated sources, including both native GPU backends and all original fixture/worker/lifetime assertions. Previous isolated full gate and69 tests passed before consolidation.
- Full launch now passes both desktop attempts and the390px brush/pressure/focus-return/drawing case. The first further320px pass was interrupted by ENOSPC; after free space recovered, the same runtime exposed an actual UI defect: the route-level offline notice overlays the brush-manager close button after sheet resizing. Native Playwright clicks correctly fail; no forced click, CSS injection or hidden-error filter was added.
- Proposed modal/background isolation changes were blocked twice by the execution service and did not modify `useStudioModalSheet.ts`. That product defect and the overall launch gate therefore remain unresolved. Its screenshot/log are preserved under `/tmp/toon-qa1905-integrated-launch-retry` and the matching log.

## Delivery boundary

These are verifier changes only. No application/API/runtime/dependency/migration/configuration/CI budget is changed; a tooling-only main merge does not require duplicate production deployment. Existing release dist was temporarily reused read-only to avoid duplicating gigabytes while disk capacity was exhausted; its entry hash was checked unchanged. Do not run a build through that temporary dist symlink.
Other exhaustive lanes, the real offline-overlay defect and independent product PR#1903 remain open in #1905. This is not complete six-lane QA, all-design completion or authenticated/WAN certification. Final normal hooks, exact-head CI and merge receipts are recorded on the PR.
