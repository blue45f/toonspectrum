# 운세 외부 데이터 보강 운영 가이드

검토 기준: 2026-09-20. 기존 29개 관측소 경험과 캐릭터 POST API를 보존합니다. 외부 제공자는 기본 비활성입니다. 이 변경은 키 발급·운영 설정·운영 배포를 수행하지 않습니다.

## 구현 범위

| 영역 | 동작 |
| --- | --- |
| 기본 계산 | 브라우저·서버 공통 로컬 엔진. 외부 데이터나 운영자 AI 키 없이 동작 |
| 타로 | 기본 메이저 22장 보존, 선택형 전체 78장, 자체 한국어 창작 질문, 소유한 벡터 모티프 사용 |
| 재현 | 기준 날짜·덱·선택 번호를 고정. 전체 덱은 중복 없는 Fisher–Yates 추출 |
| 만세력 보강 | 선택한 공용 월만 서버에 전송. KASI 응답의 음력 날짜·윤달·요일 대조 결과를 별도 표시 |
| 별자리 | 일간·주간·월간 자체 한국어 콘텐츠. 권리 승인 후 영어 원문만 별도 제공 가능 |
| 저장·공유 | 외부 원문은 로컬 해석 저장/공유에 섞지 않음. 생년월일·시간·꿈·상대 정보는 외부 제공자에 보내지 않음 |

KASI 대조 일치는 역법 항목에 한정됩니다. 운세의 예측력을 인증하거나 달력의 월건을 사주의 월주로 치환하지 않습니다. 불일치는 날짜별로 표시하고 기존 달력을 덮어쓰지 않습니다. 현재 24절기 시각은 기존 로컬 계산을 유지합니다. 특일 API는 날짜 단위의 참고 정보만 선택적으로 보강하며 절입 시각이나 원국을 덮어쓰지 않습니다.

## API

`GET /api/fortune/capabilities`는 설정상 사용 가능 여부·차단 사유를 제공합니다. 이는 제공자 실시간 가용성 보증이 아닙니다.

`GET /api/fortune/calendar?month=2024-02`는 공용 월의 대조 결과를 반환합니다. `checks[].fields`는 **불일치 항목**입니다. 전 일자·중복·페이지·업무 오류 코드·XML 구조를 검사합니다.

`GET /api/fortune/horoscope?sign=aries&period=daily&date=2026-09-20`는 기본 콘텐츠 또는 승인된 추가 원문을 반환합니다. 기간은 `daily`, `weekly`, `monthly`입니다. 알 수 없는 파라미터·개인정보 필드·잘못된 실제 날짜는 400으로 거절합니다.

모든 GET은 HTTP `no-store`이며 읽기 동작에 활동 보상을 지급하지 않습니다. 상태는 `local`, `external`, `external-cache`, `local-fallback`으로 구분합니다.

## 활성화 조건

환경변수 예시는 `.env.fortune.example`에 있습니다. 자동 적용되지 않으며 운영 설정 변경에는 별도 승인이 필요합니다.

KASI는 공공데이터포털에서 음양력 정보 활용 승인을 받은 **디코딩 키**를 서버에 등록해야 합니다. `FORTUNE_KASI_ENABLED=true`와 기존 Upstash coordination 설정이 모두 필요합니다. 키가 없거나 분산 예산을 확인할 수 없으면 외부 호출을 하지 않습니다.

별자리는 제공자의 상업적 표시·서버 캐시 권한과 실제 호출 한도를 확인하고 내부 승인 기록을 남긴 뒤에만 `FORTUNE_HOROSCOPE_ENABLED`, `FORTUNE_HOROSCOPE_DISPLAY_AND_CACHE_APPROVED`, `FORTUNE_HOROSCOPE_POLICY_REVISION`을 설정합니다. 무료라는 소개만으로 재배포 권한을 승인하지 않습니다. 번역·각색·공유 권한은 이 스위치에 포함되지 않으며 해당 기능을 구현하지 않았습니다.

영어 원문은 공식 문서의 `data.date`, `data.period`, `data.sign`, `data.horoscope`를 검사합니다. 표기 날짜가 요청 날짜와 일치하지 않거나 과거 날짜 요청이면 로컬 콘텐츠로 대체합니다. 공급자의 시간대와 주·월 경계가 불명확하므로 한국 표준시와 동일하다고 보증하지 않습니다. 다른 날짜 형식은 추측하지 않고 거절합니다.

## 비용·장애 경계

프로세스당 공용 캐시는 최대 72개, 동시 수집은 최대 16개이며 동일 키의 요청은 합칩니다. 기본은 **요청 시 수집 + 인스턴스별 메모리 캐시**입니다. 선택형 공유 PostgreSQL 스냅샷을 켜면 다른 인스턴스·재시작 이후에도 검증된 공용 결과를 재사용합니다. 아래 후속 활성화 절차를 따릅니다.

호출 예산과 장애 차단은 기존 Upstash coordination을 재사용합니다. 실제 HTTP 시도마다 고유 작업 ID를 사용하며 페이지별로 차감합니다. 기본 한도는 공급자별 UTC 하루 100회입니다. 실제 업체/계정의 한도와 리셋 시간, 같은 키를 쓰는 다른 시스템의 사용량을 별도 확인하고 보수적으로 설정해야 합니다. 공급자 API 비용 상한은 0이며 자동 유료 전환은 없습니다. 인프라 사용료까지 0원임을 의미하지는 않습니다.

연결 시간 제한, 최대 128KiB 응답, XML 선언/깊이/엔티티 제한, 고정 제공자 URL, 리다이렉트 거절, 스키마 검사를 적용합니다. KASI는 월 전체가 확인될 때만 결과를 채택하며 최대 4페이지를 시도합니다. 실패한 요청은 예산을 환불해 재호출하지 않습니다. 외부 문장은 다음 KST 자정을 넘겨 캐시하지 않습니다. 로컬 대체 결과 캐시는 60초입니다.

화면에서는 명시적인 확인 동작만 요청을 발생시킵니다. 취소·기간 전환·화면 종료 뒤 늦게 도착한 응답은 버리며, 기존 읽기의 원국·카드·공유 본문을 바꾸지 않습니다.

## 검증과 제한

단위/통합 검증: `pnpm exec vitest run packages/core/src/fortune apps/api/src/modules/fortune apps/web/src/domains/fortune`.
브라우저 검증: 로컬 Vite를 실행한 뒤 `FORTUNE_BASE_URL=http://127.0.0.1:5279 node e2e/fortune-enrichment-smoke.mjs` 및 기존 `e2e/fortune-observatory-smoke.mjs`를 실행합니다. 결과·스크린샷은 임시 폴더에만 기록합니다.

테스트의 KASI 응답은 합성 프로토콜 fixture이며 실제 발급 키를 사용한 역법 검증을 대신하지 않습니다. 실제 활성화 전 개발 계정으로 HTTP/XML 계약·사용 한도·키 인코딩·운영 네트워크를 확인해야 합니다. 외부 제공자의 성공/오류 응답이나 키가 든 URL을 운영 로그에 남기지 않습니다.

이번 변경에는 외부 문장 번역, 유료 사주 API, 타 서비스 AI 호출, 스케치/팔레트 자동 적용, 채용·인력 매칭 점수 연계가 포함되지 않습니다. 기존 창작 미션과 명시적 스튜디오 이동은 유지합니다.

## 확인한 원문 문서

- 한국천문연구원 음양력 정보: https://www.data.go.kr/data/15012679/openapi.do
- 한국천문연구원 특일 정보: https://www.data.go.kr/data/15012690/openapi.do
- Free Horoscope API 응답 계약: https://freehoroscopeapi.com/
- XML 파서: https://github.com/NaturalIntelligence/fast-xml-parser/tree/v5.10.1

공개 문서의 무료 안내는 호출 한도, 운영 SLA, 번역/재배포 권한을 포괄적으로 보증하지 않습니다. 외부 공급자를 끄면 기본 운세는 계속 사용할 수 있습니다.

## 후속 구현: 특일·공유 스냅샷·선택형 수집

`GET /api/fortune/special-days?month=2024-09&category=holidays`는 월별 공용 특일 데이터만 반환합니다. 분류는 `holidays`, `national-days`, `anniversaries`, `solar-terms`, `seasonal-days`입니다. 특일 서비스의 별도 활용 승인과 디코딩 키를 `FORTUNE_KASI_SPECIAL_SERVICE_KEY`에 등록하고 `FORTUNE_KASI_SPECIAL_DAYS_ENABLED=true`로 활성화합니다. 음양력 키를 자동 재사용하지 않습니다.

빈 성공 응답은 **제공된 항목 없음**이며 실제 휴일 부재를 보증하지 않습니다. 공공기관 휴일 표시는 개인 휴무·사업장 영업일 판단을 대신하지 않습니다. 절기는 날짜만 표시하고 원래의 초 단위 계산은 유지합니다. 이름·분류·페이지·날짜·중복·총 건수·XML 오류를 검증하며, 최대 100항목/4페이지입니다. KASI 음양력/특일 요청은 보수적으로 동일한 분산 일일 예산을 공유합니다.

공유 스냅샷 활성화 순서는 별도 승인된 마이그레이션 절차로 `0081_fortune_public_snapshot.sql` 및 관리형 runtime ACL을 적용한 뒤 `FORTUNE_SHARED_SNAPSHOTS_ENABLED=true`를 설정하는 것입니다. 애플리케이션 시작은 DDL을 실행하지 않습니다. runtime은 해당 테이블에 SELECT/INSERT/DELETE와 payload·checked_at·expires_at UPDATE만 필요하며 PK 변경·TRUNCATE·스키마 생성 권한을 주지 않습니다.

공유 저장소는 기존 PostgreSQL 풀을 사용합니다. 외부 검증 성공 결과만 저장하며 엄격한 스키마로 개인 입력·알 수 없는 필드를 거절합니다. 키에는 공용 연월/별자리/기간/정책 버전만 들어갑니다. 오래된 쓰기는 최신 checked_at을 덮어쓸 수 없습니다. 저장소 장애·수집 잠금 충돌·잠금 소유권 상실 시 외부 호출 우회 없이 로컬 결과로 대체합니다. 분산 잠금은 60초이며, 수집 후 소유권을 다시 확인합니다.

달력/특일 TTL은 24시간, 별자리 TTL은 최대 1시간이며 KST 자정을 넘기지 않습니다. DB 읽기와 UI 응답에 만료·출처·문맥 검증을 적용합니다. 설정이 꺼지거나 권한 승인이 해제되면 기존 캐시보다 설정을 먼저 확인합니다. DB 클라이언트 대기 제한은 2초이며 서버 측 SQL 실행 중단을 보증하는 설정은 아닙니다. 기존 풀의 연결 제한도 적용됩니다.

`FORTUNE_REFRESH_ENABLED=true`는 공유 스냅샷을 요구합니다. 시작 30초 후, 이후 매일 01:15 KST에 현재/다음 달 음양력·공휴일·24절기를 최대 6개 데이터셋으로 순차 준비합니다. 유효한 캐시는 재사용하며, 하나라도 로컬 대체 결과이면 다음 외부 작업을 중단합니다. 여러 인스턴스는 데이터셋별 분산 잠금을 공유합니다. 프로세스 중단 동안 실행을 보장하는 외부 스케줄러가 아니라 보조 타이머이므로 조회 경로가 cold-start 복구 역할을 유지합니다.

성공한 수집 주기 후 만료 스냅샷을 최대 1,000건 삭제합니다. 수집을 끈 채 공유 저장소만 운영하는 경우 승인된 운영 절차에서 만료 행 정리를 별도로 수행해야 합니다. 수집·공유 저장소는 PostgreSQL/Upstash 인프라 사용량을 발생시키므로 무비용 인프라로 표현하지 않습니다. 새 유료 서비스·원문 번역·자동 배포는 추가하지 않았습니다.

화면은 접을 수 있는 특일 패널에서 명시적 확인에만 요청하며, 취소·분류 전환·연월 전환 뒤 지연 응답을 버립니다. 기본 달력·카드·사주·해석 저장/공유는 변경하지 않습니다.

실제 SQL 검증은 `FORTUNE_TEST_DATABASE_URL`로 명시한 loopback의 `fortune_disposable_*` 전용 DB에서만 실행합니다. 테스트가 생성한 무작위 schema와 제한된 runtime role만 사용합니다. 운영 dotenv를 읽거나 운영 마이그레이션을 적용하지 않습니다. API 프로토콜 fixture는 합성 응답이며 실제 KASI 키 가용성·자료 정확성을 검증한 증거가 아닙니다.
