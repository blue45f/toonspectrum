# 🎨 ToonSpectrum Creator Studio — 사용자 종합 매뉴얼 & 튜토리얼 Guide

ToonSpectrum 스튜디오는 2D 드로잉, 3D 마네킹/CAD 배경 선화 추출, VRM 포즈 트레이싱 및 웹캠 모션 캡처를 한곳에서 처리하는 웹 기반 전문 웹툰 저작 환경입니다.

> 📘 **상세 3D DCC 하이브리드 아키텍처 매뉴얼**: [STUDIO_3D_DCC_MANUAL.md](STUDIO_3D_DCC_MANUAL.md) 문서에서 다중 기하 커널, SketchUp 추론 스냅, Shot Override 및 포맷 매니페스트를 확인할 수 있습니다.

---

## 📌 목차 (Table of Contents)

1. [스튜디오 개요 및 워크스페이스 레이아웃](#1-스튜디오-개요-및-워크스페이스-레이아웃)
2. [2D 디지탈 드로잉 & 수채화/유화 인크 캔버스](#2-2d-디지탈-드로잉--수채화유화-인크-캔버스)
3. [3D 마네킹 & VRM 포즈 트레이싱 시스템](#3-3d-마네킹--vrm-포즈-트레이싱-시스템)
4. [캐릭터 셰이퍼 (프리셋 캐릭터 작업실)](#4-캐릭터-셰이퍼-프리셋-캐릭터-작업실)
5. [웹캠 모션 캡처 & 손가락/표정 튜닝 가이드](#5-웹캠-모션-캡처--손가락표정-튜닝-가이드)
6. [3D 인테리어/건축 배경 CAD 선화 추출기](#6-3d-인테리어건축-배경-cad-선화-추출기)
7. [스마트 갭 필 (Gap Fill) & 렌더링 필터](#7-스마트-갭-필-gap-fill--렌더링-필터)
8. [웹툰 컷 분할 및 포맷 내보내기 (Export)](#8-웹툰-컷-분할-및-포맷-내보내기-export)

---

## 1. 스튜디오 개요 및 워크스페이스 레이아웃

ToonSpectrum 스튜디오는 크게 4개의 핵심 영역으로 구성되어 있습니다:

- **좌측 툴바 (Tool Rail)**: 브러시, 지우개, 채우기(Fill), 3D 카메라, 레이소(Lasso) 선택, 텍스트 및 3D 씬 삽입 도구.
- **중앙 캔버스 (Canvas)**: 문서 표시와 포인터 입력은 Konva Stage가, 획 픽셀 커밋은 그 위의 Canvas2D 종단 경로가 소유합니다. WebGPU/WASM 엔진은 게이트된 island에서만 동작합니다 — 전용 WebGPU 브러시 레인, Hokusai 자연매체(WASM), Vello 문서 벡터 island, Pixi 선택 오버레이. 무제한 줌 및 회전, R8 알파 맵 브러시 스트로크 렌더링을 지원합니다. 어떤 엔진이 무엇을 소유하는지는 생성 문서 [docs/engines/renderer-roles.md](docs/engines/renderer-roles.md)가 단일 진실 원천입니다.
- **우측 레이어 및 3D 속성 패널 (Inspector)**: 래스터 레이어 합성, 3D VRM 마네킹 조명, 카메라 시점, FX 필터 조절.
- **하단 타임라인 & 3D 컨트롤 바 (Control Bar)**: 컷 애니메이션, 웹캠 포즈 스트리밍 상태, 프레임 레이트 및 프리셋 가젯.

---

## 2. 2D 디지탈 드로잉 & 수채화/유화 인크 캔버스

### 브러시 엔진 (Dynamic Brush & Wet Ink)
1. **브러시 선택**: 좌측 툴바에서 브러시 아이콘을 클릭하거나 단축키 `B`를 누릅니다.
2. **수채화/Wet Ink 효과**: 우측 속성 패널에서 `Wet Ink Engine`을 활성화하면 물감이 마르기 전 캔버스 위에서 번지는 효과와 스트로크 종단부 오파시티 믹싱을 경험할 수 있습니다.
3. **QuickShape (손떨림 보정 및 자동 도형)**: 선을 그린 후 끝점에서 0.3초간 펜을 멈추면 직각 직선, 원, 다각형이 자동으로 정교한 원형/정사각형으로 교정됩니다.

---

## 3. 3D 마네킹 & VRM 포즈 트레이싱 시스템

인체 데생이나 포즈 잡기가 어려울 때 3D VRM 캐릭터 마네킹을 캔버스 위에 소환하여 자유롭게 구도를 잡을 수 있습니다.

1. **3D 모델 소환**: 상단 메뉴 `삽입` → `3D VRM 마네킹`을 선택합니다.
2. **포즈 편집**:
   - 3D 마네킹 관절(손목, 팔꿈치, 어깨, 무릎, 골반)을 캔버스 상에서 직접 클릭 드래그하여 포즈를 변경합니다.
   - 사전 정의된 20여 종의 웹툰 포즈 라이브러리(달리기, 검술, 대화, 앉기, 공중점프 등)를 클릭 한 번으로 적용합니다.
3. **툰 셰이딩 (Toon Shaded Mode)**: 우측 3D 셰이딩 설정에서 `Toon Outline`을 켜면 3D 캐릭터가 만화풍 2D 외곽선과 카툰 섀도우로 자동 실시간 변환됩니다.

---

## 4. 캐릭터 셰이퍼 (프리셋 캐릭터 작업실)

포즈만이 아니라 **캐릭터 자체를 만드는** 화면입니다. 상단 메뉴 `3D` → `캐릭터 셰이퍼`,
왼쪽 도구 레일의 `캐릭터 셰이퍼`, 또는 주소 `/studio/character` 로 엽니다.

1. **프리셋 카드로 조형**: 얼굴형·눈·눈동자·코·입·귀·헤어·체형·상의·하의·신발·액세서리·표정·포즈·
   손 포즈 15개 슬롯을 카드로 고릅니다. 한 번 누르면 즉시 적용되고 되돌리기 한 단계가 됩니다.
   모델이 지원하지 않는 항목은 이유를 카드에 적고 몰래 바꿔치기하지 않습니다.
2. **AI 추천 · 사진 · 웹캠**: 참고 이미지를 올리면 레시피와 팔레트를 제안하고, 사진이나 웹캠으로
   포즈를 옮깁니다. 전부 기기 안에서 처리합니다.
3. **표면 드로잉**: 모델 표면에 직접 칠합니다(`B`).
4. **내보내기**: 투명 배경 PNG로 캔버스에 바로 넣거나, 밑색·음영·하이라이트·주선이 나뉜 레이어
   PSD로 내보냅니다. 만들지 못한 레이어는 이유와 함께 목록에 남습니다.
5. **고급 편집**: 기존 3D 캐릭터 대화상자를 같은 모델 위에서 그대로 엽니다. 「셰이퍼로 돌아가기」로
   되돌아옵니다.

슬롯 표, 단축키, PSD 레이어 순서, Blender 캐릭터 패키지 불러오기, 정직한 한계는
[`docs/studio/character-shaper.md`](docs/studio/character-shaper.md) 에 정리되어 있습니다.

---

## 5. 웹캠 모션 캡처 & 손가락/표정 튜닝 가이드

노트북이나 PC의 기본 웹캠을 통해 작가 자신의 동작과 손가락 제스처를 3D 마네킹에 실시간 이식합니다.

### 🎥 웹캠 모션 캡처 시작 단계
1. **모션 캡처 켜기**: 우측 3D 속성 패널에서 `웹캠 모션 캡처 시작` 버튼을 클릭합니다.
2. **카메라 권한 승인**: 브라우저의 웹캠 접근 권한을 승인합니다.
3. **포즈 실시간 추적**:
   - 카메라 앞에 서면 AI Pose Detector가 신체 33개 관절 랜드마크를 실시간 추적하여 3D 마네킹을 내 몸 움직임대로 구동합니다.
   - **Hand Pose Solver**: 손가락 21개 마디 랜드마크를 추적하여 정교한 손 동작(주먹 쥐기, 펜 잡기, 삿대질 등)을 손쉽게 만듭니다.

### 💡 웹캠 모션 캡처 200% 활용 팁
- **어려운 인체 구도 1초 완성**: 웹캠 앞에서 원하는 자세를 취한 후 `포즈 캡처 락(Lock)`을 누르면 3D 마네킹이 해당 포즈로 고정됩니다.
- **카메라 앵글 조작**: 포즈가 고정된 상태에서 3D 뷰포트를 회전시켜 하이앵글(위에서 내려다보는 컷)이나 로우앵글(아래에서 올라다보는 격투 컷)로 자유롭게 앵글을 바꿉니다.
- **선화 투영 (Tracing Guide)**: 3D 마네킹 레이어의 투명도(Opacity)를 30%로 낮춘 뒤 상단 드로잉 레이어에서 펜으로 트레이싱하면 완벽한 인체 비율의 만화 컷을 완성할 수 있습니다.

---

## 6. 3D 인테리어/건축 배경 CAD 선화 추출기

3D 아파트, 교실, 거리 씬(.STEP, .IGES, .OBJ, .GLTF) 모델을 불러와 2D 만화 배경 선화로 변환합니다.

1. **3D 에셋 불러오기**: `3D 배경 삽입` 선택 후 파일 업로드.
2. **Line Art Extractor (선화 추출)**: 우측 패널에서 `선화 추출 (Extract Line Art)`을 클릭하면 3D 메쉬의 엣지를 감지하여 2D 외곽선 레이어를 깔끔하게 분리 추출해 줍니다.
3. **선 굵기 & 톤 설정**: 추출된 선화 레이어의 Line Thickness 및 Tone Gradient를 조절하여 웹툰 배경에 적합하게 튜닝합니다.

---

## 7. 스마트 갭 필 (Gap Fill) & 렌더링 필터

- **Smart Gap Fill**: 펜 선이 약간 미세하게 뚫려 있어도 색상 채우기(Bucket Tool) 시 물감이 바깥으로 새어나가지 않도록 구멍을 자동 감지하여 닫아줍니다.
- **Visual FX Filters**: 캔버스 전체 또는 특정 레이어에 비네팅, 색수차(Chromatic Aberration), 블러, 툰 그레인(Grain) 필터를 덧씌워 극적인 웹툰 연출을 완성합니다.

---

## 8. 웹툰 컷 분할 및 포맷 내보내기 (Export)

1. **컷 프레임 분할**: 컷 분할 툴로 캔버스를 세로 웹툰 스타일로 분할합니다.
2. **포맷 선택**: `파일` → `내보내기` 메뉴에서 `PNG`, `JPEG`, `WebP` 또는 네이버/카카오 웹툰 규격 원고(가로 690px / 1440px 세로 자동 절편 PNG)로 분할 다운로드합니다.
