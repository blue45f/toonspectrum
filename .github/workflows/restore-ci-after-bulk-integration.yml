name: Restore full CI after owner-authorized bulk integration

on:
  push:
    branches:
      - restore/ci-after-bulk-integration-20260909

permissions:
  contents: read

jobs:
  core:
    name: core
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - name: Record one-time restore gate
        run: echo "The original full CI workflow is restored in this commit; execution is intentionally deferred for this one-time protected-branch update."
