name: Retire macOS queue cancellation helper

on:
  push:
    branches:
      - ci/actions-queue-dedupe-20260908
    paths:
      - .github/workflows/one-shot-prioritize-final-core-macos.yml

permissions: {}

concurrency:
  group: one-shot-prioritize-final-core-macos
  cancel-in-progress: true

jobs:
  retire:
    if: ${{ false }}
    runs-on: macos-15
    steps:
      - run: echo "Dynamic macOS queue cancellation is retired."
