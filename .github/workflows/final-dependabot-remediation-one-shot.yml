name: Final Dependabot remediation one-shot

on:
  push:
    branches:
      - security/final-dependabot-remediation-20260908-v3

permissions:
  contents: write

concurrency:
  group: final-dependabot-remediation-20260908-v3
  cancel-in-progress: true

jobs:
  remediate:
    if: ${{ !contains(github.event.head_commit.message, '[skip final-dependabot-fix]') }}
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - name: Check out dependency manifests from the final integration tree
        uses: actions/checkout@v6
        with:
          ref: security/final-dependabot-remediation-20260908-v3
          fetch-depth: 1
          filter: blob:none
          sparse-checkout-cone-mode: false
          sparse-checkout: |
            package.json
            pnpm-lock.yaml
            pnpm-workspace.yaml
            apps/*/package.json
            packages/*/package.json
            patches/**
            .github/workflows/final-dependabot-remediation-one-shot.yml

      - name: Set up pnpm
        uses: pnpm/action-setup@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm

      - name: Apply every available advisory update to the final dependency graph
        shell: bash
        run: |
          set -uo pipefail
          mkdir -p /tmp/final-dependabot-fix

          set +e
          pnpm audit --json --audit-level low \
            > /tmp/final-dependabot-fix/before.json \
            2> /tmp/final-dependabot-fix/before.err
          printf '%s\n' "$?" > /tmp/final-dependabot-fix/before.exit

          pnpm audit --fix=update --audit-level low \
            > /tmp/final-dependabot-fix/fix.log 2>&1
          printf '%s\n' "$?" > /tmp/final-dependabot-fix/fix.exit

          pnpm audit --json --audit-level low \
            > /tmp/final-dependabot-fix/after.json \
            2> /tmp/final-dependabot-fix/after.err
          printf '%s\n' "$?" > /tmp/final-dependabot-fix/after.exit
          set -e

          cat /tmp/final-dependabot-fix/fix.log

      - name: Require zero advisories at every severity
        shell: bash
        run: |
          node --input-type=module <<'NODE'
          import { readFileSync } from "node:fs";

          function readJson(path) {
            return JSON.parse(readFileSync(path, "utf8"));
          }
          function exitCode(path) {
            return Number(readFileSync(path, "utf8").trim());
          }
          function counts(report) {
            const source = report?.metadata?.vulnerabilities ?? {};
            const normalized = Object.fromEntries(
              ["info", "low", "moderate", "high", "critical"].map((severity) => [
                severity,
                Number(source[severity] ?? 0),
              ]),
            );
            normalized.total = Object.values(normalized).reduce(
              (sum, value) => sum + value,
              0,
            );
            return normalized;
          }

          const before = readJson("/tmp/final-dependabot-fix/before.json");
          const after = readJson("/tmp/final-dependabot-fix/after.json");
          const summary = {
            before: counts(before),
            after: counts(after),
            fixExitCode: exitCode("/tmp/final-dependabot-fix/fix.exit"),
            auditExitCode: exitCode("/tmp/final-dependabot-fix/after.exit"),
          };
          console.log(JSON.stringify(summary, null, 2));

          if (summary.auditExitCode !== 0 || summary.after.total !== 0) {
            console.error(readFileSync("/tmp/final-dependabot-fix/after.err", "utf8"));
            console.error(JSON.stringify(after, null, 2));
            process.exit(1);
          }
          NODE

      - name: Commit only the final dependency remediation
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          unexpected="$({ git diff --name-only; git diff --cached --name-only; } | sort -u | grep -Ev '^(package\.json|pnpm-lock\.yaml|pnpm-workspace\.yaml|apps/[^/]+/package\.json|packages/[^/]+/package\.json|\.github/workflows/final-dependabot-remediation-one-shot\.yml)$' || true)"
          if [[ -n "$unexpected" ]]; then
            echo "Unexpected tracked changes:" >&2
            echo "$unexpected" >&2
            exit 1
          fi

          git add -- \
            package.json \
            pnpm-lock.yaml \
            pnpm-workspace.yaml \
            ':(glob)apps/*/package.json' \
            ':(glob)packages/*/package.json'
          git rm -- .github/workflows/final-dependabot-remediation-one-shot.yml
          git diff --cached --check
          git status --short
          git commit -m "fix(security): resolve final dependency advisories [skip final-dependabot-fix]"
          git push origin HEAD:security/final-dependabot-remediation-20260908-v3
