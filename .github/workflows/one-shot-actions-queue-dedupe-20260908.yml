name: Retire Actions queue cancellation helper

on:
  push:
    branches:
      - ci/actions-queue-dedupe-20260908
    paths:
      - .github/workflows/one-shot-actions-queue-dedupe-20260908.yml

permissions: {}

concurrency:
  group: one-shot-actions-queue-dedupe-20260908
  cancel-in-progress: true

jobs:
  retire:
    if: ${{ false }}
    runs-on: ubuntu-slim
    steps:
      - run: echo "Dynamic queue cancellation is retired."
