name: Retire superseded Studio collaboration validation

on:
  push:
    branches:
      - ops/production-final-2015ef35-20260908
    paths:
      - .github/workflows/studio-mesh-sync-repair.yml

permissions:
  contents: read

concurrency:
  group: studio-mesh-sync-ops/production-final-2015ef35-20260908
  cancel-in-progress: true

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by protected-main integration PR #881"
