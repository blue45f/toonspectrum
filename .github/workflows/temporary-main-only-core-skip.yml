name: Temporary main-only convergence core skip

# Explicitly retained only until the main-only branch convergence completes.
on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/temporary-main-only-core-skip.yml

permissions:
  contents: read

jobs:
  core:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Explicit temporary skip for main-only convergence.'
