name: Retire Windows queue cancellation helper

on:
  push:
    branches:
      - ci/actions-queue-dedupe-20260908
    paths:
      - .github/workflows/one-shot-prioritize-core-907-windows.yml

permissions: {}

concurrency:
  group: one-shot-prioritize-final-core-windows
  cancel-in-progress: true

jobs:
  retire:
    if: ${{ false }}
    runs-on: windows-latest
    steps:
      - run: echo "Dynamic Windows queue cancellation is retired."
