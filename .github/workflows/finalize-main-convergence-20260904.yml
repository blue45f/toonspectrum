name: Finalize main convergence and branch cleanup

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Final convergence is temporarily paused while product PR validation is repaired."
