name: Retire stale Studio ink PR 882 run

on:
  push:
    branches:
      - ci/actions-queue-dedupe-20260908
    paths:
      - .github/workflows/one-shot-preempt-studio-ink-882.yml

permissions: {}

concurrency:
  group: studio-ink-live-commit-Studio ink live-commit fidelity-refs/pull/882/merge
  cancel-in-progress: true

jobs:
  retire:
    if: ${{ false }}
    runs-on: ubuntu-slim
    steps:
      - run: echo "Retired stale Studio ink run from closed PR 882."
