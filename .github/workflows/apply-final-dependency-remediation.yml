name: Retired final dependency remediation helper

on:
  push:
    branches:
      - automation/final-dependency-remediation-20260908
    paths:
      - .github/workflows/apply-final-dependency-remediation.yml

concurrency:
  group: apply-final-dependency-remediation
  cancel-in-progress: true

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dependency remediation already completed on the final integration branch."
