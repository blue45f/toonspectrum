name: Temporary CI repair publisher cleanup target

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This placeholder is removed by the queued repair publisher."
