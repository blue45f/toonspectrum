name: Retire stale Studio asset PR 883 run

on:
  push:
    branches:
      - ci/actions-queue-dedupe-20260908
    paths:
      - .github/workflows/one-shot-preempt-studio-asset-883.yml

permissions: {}

concurrency:
  group: studio-asset-browser-883
  cancel-in-progress: true

jobs:
  retire:
    if: ${{ false }}
    runs-on: ubuntu-slim
    steps:
      - run: echo "Retired stale Studio asset run from closed PR 883."
