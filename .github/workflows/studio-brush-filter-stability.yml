name: Retire superseded Studio brush and filter validation

on:
  push:
    branches:
      - merge-ready/3ad5d69c-final
    paths:
      - .github/workflows/studio-brush-filter-stability.yml

permissions:
  contents: read

concurrency:
  group: studio-brush-filter-stability-867
  cancel-in-progress: true

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by protected-main integration PR #881"
