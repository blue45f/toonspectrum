name: Retire stale BG3D PR 881 run

on:
  push:
    branches:
      - ci/actions-queue-dedupe-20260908
    paths:
      - .github/workflows/one-shot-preempt-bg3d-881.yml

permissions: {}

concurrency:
  group: bg3d-runtime-881
  cancel-in-progress: true

jobs:
  retire:
    if: ${{ false }}
    runs-on: ubuntu-slim
    steps:
      - run: echo "Retired stale BG3D run from closed PR 881."
