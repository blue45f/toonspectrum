name: ToonStudio session goals

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
    paths:
      - "apps/web/src/app/routes/groups/**"
      - "apps/web/src/domains/creator/**"
      - "apps/web/src/shared/components/**"
      - "scripts/verify-toonstudio-session-goals.mjs"
      - ".github/workflows/toonstudio-session-goals.yml"
      - "eslint.config.mjs"
      - "vitest.config.*"
      - "package.json"
      - "pnpm-lock.yaml"

permissions:
  contents: read

concurrency:
  group: toonstudio-session-goals-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  focused-validation:
    name: focused validation
    runs-on: ubuntu-24.04
    timeout-minutes: 24
    env:
      CI: "true"
      HUSKY: "0"
      NODE_OPTIONS: --max-old-space-size=8192
    steps:
      # Related tests validate tracked documentation evidence; retain docs.
      - name: Checkout focused ToonStudio source
        uses: actions/checkout@v6
        with:
          persist-credentials: false
          fetch-depth: 1
          filter: blob:none
          sparse-checkout-cone-mode: false
          sparse-checkout: |
            /*
            !/apps/web/public/assets/
            /apps/web/public/assets/3d/environments/refined-v6/
            /apps/web/public/assets/3d/environments/expansion-v1/
            /apps/web/public/assets/virtual-studio/world/default-world.json
            /apps/web/public/assets/create/creator-board-hero.png
            /apps/web/public/assets/studio/generated-backgrounds/gpt25-v1/sf/gpt25-bg-sf-research-lab-vertical-depth/background.png
            /apps/web/public/assets/3d/characters/thumbnails/refined-v2/
            !/apps/web/public/vrm/
            !/artifacts/
            !/tests/benchmarks/results/
            /tests/benchmarks/results/v12-runtime-fault-matrix.json

      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11.4.0

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --prefer-offline

      - name: Restore focused lint cache
        uses: actions/cache@v4
        with:
          path: node_modules/.cache/eslint/quick
          key: toonstudio-session-eslint-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('pnpm-lock.yaml', 'eslint.config.mjs') }}-${{ github.sha }}
          restore-keys: |
            toonstudio-session-eslint-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('pnpm-lock.yaml', 'eslint.config.mjs') }}-

      - name: Verify planned feature ownership and UI reachability
        run: node scripts/verify-toonstudio-session-goals.mjs

      - name: Fetch the exact PR base
        if: github.event_name == 'pull_request'
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: git fetch --no-tags --depth=1 origin "$BASE_SHA"

      - name: Collect changed ToonStudio sources
        if: github.event_name == 'pull_request'
        shell: bash
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          set -euo pipefail
          git diff --name-only -z --diff-filter=ACMR "$BASE_SHA" HEAD -- \
            'apps/web/src/**/*.js' \
            'apps/web/src/**/*.jsx' \
            'apps/web/src/**/*.ts' \
            'apps/web/src/**/*.tsx' \
            'scripts/**/*.js' \
            'scripts/**/*.mjs' \
            'scripts/**/*.mts' > "$RUNNER_TEMP/toonstudio-related-files"

      - name: Lint changed ToonStudio sources
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          mapfile -d '' -t files < "$RUNNER_TEMP/toonstudio-related-files"
          if ((${#files[@]} == 0)); then
            echo "No changed ToonStudio source files require lint."
            exit 0
          fi
          pnpm exec eslint --max-warnings=0 --no-warn-ignored \
            --cache --cache-strategy content \
            --cache-location node_modules/.cache/eslint/quick/ \
            -- "${files[@]}"

      - name: Run tests related to changed ToonStudio sources
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          mapfile -d '' -t files < "$RUNNER_TEMP/toonstudio-related-files"
          if ((${#files[@]} == 0)); then
            echo "No changed ToonStudio source files require related tests."
            exit 0
          fi
          pnpm exec vitest related "${files[@]}" --run --pool=forks --maxWorkers=2

      - name: Run manual focused smoke suite
        if: github.event_name == 'workflow_dispatch'
        run: |
          pnpm exec vitest run \
            apps/web/src/domains/creator/studio-quick-task-workspaces.test.ts \
            apps/web/src/domains/creator/studio-shell/StudioQuickStart.test.tsx \
            apps/web/src/domains/creator/studio-shell/StudioProjectLibraryPage.test.tsx \
            --pool=forks \
            --maxWorkers=2

  repair-skia-host:
    name: one-shot Skia host-boundary repair
    if: github.event_name == 'pull_request' && github.head_ref == 'fix/skia-live-input-main-reconcile-20260923'
    permissions:
      contents: write
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    env:
      CI: "true"
      HUSKY: "0"
      NODE_OPTIONS: --max-old-space-size=12288
    steps:
      - name: Checkout exact PR head branch
        uses: actions/checkout@v6
        with:
          ref: fix/skia-live-input-main-reconcile-20260923
          fetch-depth: 0
          persist-credentials: true

      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11.4.0

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --prefer-offline

      - name: Extract committed-ink runtime from the editor host
        run: python3 .github/scripts/fix_skia_runtime_host_boundary.py

      - name: Verify exact repaired behavior and repository boundaries
        run: |
          set -euo pipefail
          pnpm exec vitest run \
            apps/web/src/domains/creator/studio-skia-committed-ink-bridge.test.ts \
            apps/web/src/domains/creator/render/StudioSkiaDocumentSurface.test.tsx \
            apps/web/src/domains/creator/render/studio-webgpu-advanced-live-integration.test.ts \
            apps/web/src/domains/creator/studio-host-architecture-ratchet.test.ts \
            --maxWorkers=2
          pnpm exec eslint \
            apps/web/src/domains/creator/StudioCuttoonEditorHost.tsx \
            apps/web/src/domains/creator/use-studio-skia-committed-ink-host-runtime.ts \
            apps/web/src/domains/creator/studio-skia-committed-ink-bridge.test.ts \
            --max-warnings=0
          pnpm run typecheck
          pnpm run validate:architecture
          test "$(wc -l < apps/web/src/domains/creator/StudioCuttoonEditorHost.tsx | tr -d ' ')" -le 29696
          git diff --check

      - name: Commit only the verified product repair
        env:
          TARGET_BRANCH: fix/skia-live-input-main-reconcile-20260923
        run: |
          set -euo pipefail
          git fetch --no-tags origin main
          git show origin/main:.github/workflows/toonstudio-session-goals.yml > .github/workflows/toonstudio-session-goals.yml
          rm .github/scripts/fix_skia_runtime_host_boundary.py
          git config user.name "OpenAI Codex"
          git config user.email "codex@openai.com"
          git add -A
          git commit -m "refactor(studio): isolate Skia committed-ink host runtime"
          git push origin "HEAD:${TARGET_BRANCH}"
