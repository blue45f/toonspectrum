# Temporary Actions queue drain

This draft branch exists only to run a reviewed, path-scoped queue-drain workflow while preserving current `main` and exact open pull-request heads. It must not be merged. The workflow and this marker will be removed after the drain attempt is complete.
