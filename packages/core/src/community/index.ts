// @toonspectrum/core/community — 웹·토스가 공유하는 커뮤니티 코어 배럴.
// (A) types: 읽기전용 리뷰 피드(GET /api/reviews) 응답 계약.
// (B) store: 작성형 데모 커뮤니티(채널/게시판/카페/댓글)의 순수 localStorage 엔진.

export * from "./types";
export * from "./store";
