# Claude Code 저장소 지침

이 파일은 도구용 어댑터다. 정책의 단일 기준은 루트 `AGENTS.md`다.

1. 작업 시작 전 루트 `AGENTS.md`를 읽는다.
2. 수정 파일의 상위 경로에 더 가까운 `AGENTS.md`가 있으면 함께 적용한다.
3. 커밋·PR·문서·주석·작업 보고는 한글을 기본으로 한다.
4. 관련 없는 변경을 건드리지 않고 기존 worktree와 사용자 변경을 보존한다.
5. 구현 전 완료 조건과 검증 명령을 정한다.
6. 완료 전 `pnpm harness:verify`와 영향 영역 테스트를 실행한다.
7. 운영 배포는 `AGENTS.md`와 `DEPLOY.md`의 별도 승인 정책 없이는 수행하지 않는다.

규칙을 이 파일에 복제하지 않는다. 충돌 시 `AGENTS.md`가 우선한다.
