{
  "name": "@toonspectrum/desktop-sync-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-folder watcher and durable sync journal for ToonStudio desktop packaging.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config vitest.config.ts"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "typescript": "^6.0.2",
    "vitest": "4.1.11"
  }
}
