# Admin platform

Infrastructure adapters for the Admin application belong here: HTTP client setup, telemetry, configuration and browser/runtime adapters.

Platform code may support domains but must not become a second location for business rules.
