import { StudioWorkspacePage } from "../creator/workspace/StudioWorkspacePage";

/** A single studio home; spatial and list views share the same work context. */
export function CreatorHomePage() {
  return <StudioWorkspacePage />;
}
