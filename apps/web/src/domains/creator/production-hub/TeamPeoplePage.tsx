import { TeamWorkspacePage } from "./TeamWorkspacePage";

/** Canonical people-and-access route; legacy workspace routes render the same authority. */
export function TeamPeoplePage() {
  return <TeamWorkspacePage />;
}
