/**
 * Compatibility export for web call sites.
 * The authoritative creator-role contract lives in the shared core package so
 * browser and API runtimes validate the same versioned profile shape.
 */
export * from "@toonspectrum/core/creator-role";
