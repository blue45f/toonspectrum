# Temporary source-only patch transport. Reject stale sources and verify exact reviewed outputs.
import hashlib
import json
from pathlib import Path

updates = json.loads(r'''
[["apps/web/src/domains/creator/StudioCuttoonEditorHost.tsx","9b8eee356f02d8d028e032e2ecd31014894af819cb199b7ab57808a6cced5b53","fda169ecc097002b45f6d03f77d7e24a556c4829aa4f2f1a43a63ea05597d980",[[0,0,"import { createStudioAutosaveSnapshotFence } from \"./studio-autosave-snapshot-fence\";\n"],[1005,1005,"  normalizeStudioReleaseScheduleDeferred,\n"],[7253,7253,"    const snapshotIsCurrent = createStudioAutosaveSnapshotFence({\n      generation: scheduledGeneration,\n      pendingFingerprint: scheduledPendingFingerprint,\n      session: scheduledAutosaveSession,\n    }, () => ({\n      generation: studioRevisionProjectGenerationRef.current,\n      pendingFingerprint: studioPendingStrokeFingerprint(pendingStrokeCommitsRef.current),\n      session: autosaveOpfsSessionRef.current,\n    }));\n    const canPublishSnapshot = () => editorMountedRef.current\n      && !collaborationAccessRef.current.locked\n      && snapshotIsCurrent();\n"],[7271,7271,"      if (!canPublishSnapshot()) {\n        // Capture fresh ref-backed history instead of assigning a newer timestamp to old ink.\n        if (editorMountedRef.current && autosaveOpfsSessionRef.current === scheduledAutosaveSession) {\n          setAutosaveRetryNonce((current) => current + 1);\n        }\n        return;\n      }\n"],[7300,7301,"            !canPublishSnapshot()\n"],[7324,7325,"          if (!canPublishSnapshot()) return;\n"],[7332,7332,"          if (!canPublishSnapshot()) return;\n"],[7370,7370,"          if (!canPublishSnapshot()) return;\n"],[7388,7388,"          if (!canPublishSnapshot()) return;\n"],[14455,14455,"  const [sourceHydrationAttempt, setSourceHydrationAttempt] = useState(0);\n  const [workHydrationError, setWorkHydrationError] = useState<string | null>(null);\n  const retrySourceHydration = useCallback(() => {\n    setSourceHydrationAttempt((attempt) => attempt + 1);\n  }, []);\n\n"],[14461,14461,"    setWorkHydrationError(null);\n"],[14478,14478,"    // A null session before cookie verification is NOT an anonymous reader. Keep the source\n    // locked until authentication settles instead of probing a private draft via public getWork.\n    if (!studioAuthReady) return;\n"],[14515,14523,"    void loadStudioWork()\n      .then(async ({ work: w, remixAuthorName, shared, detail }) => {\n"],[14549,14549,"        const normalizedReleaseSchedule = remixId\n          ? createEmptyStudioReleaseScheduleSnapshot()\n          : await normalizeStudioReleaseScheduleDeferred(doc?.releaseSchedule);\n        if (!alive || controller.signal.aborted) return;\n"],[14666,14667,"            : normalizedReleaseSchedule\n"],[14692,14693,"        const message = e instanceof Error ? e.message : \"원고를 불러오지 못했습니다.\";\n        setWorkHydrationError(message);\n        setError(message);\n"],[14724,14724,"    sourceHydrationAttempt,\n    studioAuthReady,\n"],[29409,29409,"      workHydrationError={workHydrationError}\n      onRetrySourceHydration={retrySourceHydration}\n"]]],
["apps/web/src/domains/creator/brush/StudioBrushLibrarySheet.test.tsx","4ec9e261a5e2b4c63edd80335b83bca70672ef85f66d1f89d1c6030fbe37d33d","0891a80985278685ce2bd8f1871b5d414916bbb9a4fcf6a0552a7d66eedf8dec",[[19,19,"import * as brushCatalogModule from \"./studio-brush-catalog\";\n"],[63,69,"// Synthetic inventories exercise pagination independently of the shipped 46-paint product list.\nconst LARGE_CATALOG_FIXTURE_COUNT = 123;\nfunction installLargeCatalogFixture(): void {\n  const items = Array.from({ length: LARGE_CATALOG_FIXTURE_COUNT }, (_, index) => ({\n    ...exhaustiveCatalogItems[index % exhaustiveCatalogItems.length]!,\n    id: `synthetic-brush-${index}`,\n    name: `synthetic brush ${index}`,\n  }));\n  vi.spyOn(brushCatalogModule, \"filterStudioBrushCatalogItems\").mockImplementation((options = {}) =>\n    options.query ? items.slice(0, 71) : items,\n  );\n}\n"],[216,217,"  it(`keeps the unique ${STUDIO_ALL_BRUSH_CATALOG_ITEMS.length}-entry internal renderer registry while keeping the procedural runtime lazy`, () => {\n"],[367,368,"      target: { value: \"leaf-cluster\" },\n"],[370,371,"    const result = screen.getByRole(\"button\", { name: \"잎송이 선택\" });\n"],[435,436,"      target: { value: \"leaf-cluster\" },\n"],[440,441,"    expect(screen.getByRole(\"button\", { name: \"잎송이 선택\" })).toBeTruthy();\n"],[471,472,"  it(\"renders the complete single-batch product catalog and materializes a durable selection\", async () => {\n"],[483,484,"    // Product inventory fits in one batch; large-list behavior is covered with explicit fixtures below.\n"],[487,488,"      `${Math.min(48, exhaustiveCatalogCount)}/${exhaustiveCatalogCount}개의 브러시가 표시됩니다.`\n"],[489,490,"    expect(container.querySelectorAll(\"[data-studio-brush-source]\")).toHaveLength(Math.min(48, exhaustiveCatalogCount));\n"],[495,495,"    expect(container.querySelector(\"[data-studio-brush-progressive-sentinel]\")).toBeNull();\n    expect(container.querySelector(\"[data-studio-brush-progressive-fallback]\")).toBeNull();\n"],[508,509,"    fireEvent.click(screen.getByRole(\"button\", { name: \"잎송이 선택\" }));\n"],[513,515,"        catalogId: \"leaf-cluster\",\n        catalogName: \"잎송이\",\n"],[516,518,"        defaultWidth: 34,\n        defaultOpacity: 0.78,\n"],[530,530,"    installLargeCatalogFixture();\n"],[557,558,"      `96/${LARGE_CATALOG_FIXTURE_COUNT}개의 브러시가 표시됩니다.`,\n"],[564,564,"    installLargeCatalogFixture();\n"],[605,605,"    installLargeCatalogFixture();\n"],[622,623,"      `${LARGE_CATALOG_FIXTURE_COUNT}/${LARGE_CATALOG_FIXTURE_COUNT}개의 브러시가 표시됩니다.`,\n"],[629,629,"    installLargeCatalogFixture();\n"],[639,640,"    const remainingAfterFirstBatch = LARGE_CATALOG_FIXTURE_COUNT - 48;\n"],[648,649,"      `${Math.min(96, LARGE_CATALOG_FIXTURE_COUNT)}/`,\n"],[652,653,"    fireEvent.change(screen.getByRole(\"searchbox\", { name: \"전체 브러시 검색\" }), {\n      target: { value: \"synthetic\" },\n    });\n"]]],
["apps/web/src/domains/creator/brush/StudioBrushTray.test.tsx","2e474cb57cd5d8d2dbb23dfb7fc03f09d39049cbee724c5646bd652855478594","f13cb8d05266032eade1b7fa6381238d6173ca9149f1adcd4f910fd1d0bd8a27",[[61,64,"        activeBrushId=\"leaf-cluster\"\n        favoriteBrushIds={[\"leaf-cluster\"]}\n        recentBrushIds={[\"hair-curl-ribbon\", \"ink-particle\"]}\n"],[73,74,"      name: /즐겨찾기 브러시 잎송이 · 질감/,\n"],[76,77,"      name: /최근 사용 브러시 곱슬 모발 리본 · 질감/,\n"],[79,81,"    expect(heart.getAttribute(\"data-studio-brush-chip\")).toBe(\"leaf-cluster\");\n    expect(hair.getAttribute(\"data-studio-brush-chip\")).toBe(\"hair-curl-ribbon\");\n"],[87,89,"      id: \"hair-curl-ribbon\",\n      name: \"곱슬 모발 리본\",\n"],[117,118,"        favoriteBrushIds={[quarantinedId, \"leaf-cluster\"]}\n"],[123,124,"      name: /즐겨찾기 브러시 잎송이/,\n"],[125,126,"    expect(heart.getAttribute(\"data-studio-brush-chip\")).toBe(\"leaf-cluster\");\n"]]],
["apps/web/src/domains/creator/canvas/StudioCanvasStickyBanners.tsx","1d808b50ecd97f7414144bc305ce270b1b64ea6710a95f7962556ba8e12df026","53ad264284ba7b9576ced9c03e9cfa3978fde6f780f7aa79d65dd45d4e44d744",[[76,76,"  readonly workHydrationError?: string | null;\n  readonly onRetrySourceHydration?: () => void;\n"],[114,114,"  workHydrationError,\n  onRetrySourceHydration,\n"],[299,299,"            {sourceHydrationPending && workHydrationFailed && workHydrationError ? (\n              <span role=\"alert\" className=\"mt-2 block text-xs leading-relaxed text-warn\">\n                {workHydrationError}\n              </span>\n            ) : null}\n"],[314,315,"                    if (onRetrySourceHydration) {\n                      onRetrySourceHydration();\n                    } else {\n                      globalThis.location.reload();\n                    }\n"]]],
["apps/web/src/domains/creator/canvas/StudioCanvasStickyBanners.view-workspace.test.tsx","94a68e5c83bf2b10b9ac27052a75d86ca0f941829c6b9674cbcb1ce1435f5559","2d0fce58d3d5f4fa716cfc3ef51f489e6d16aedec22e2e2480c951543e00135e",[[109,110,""],[114,114,"    expect(hudState.props?.minMagnification).toBeCloseTo(0.08, 12);\n"],[128,128,"\n\ndescribe(\"manuscript loading recovery\", () => {\n  it(\"keeps the overwrite lock and retries the source without navigating or changing pages\", () => {\n    const retry = vi.fn();\n    const context = createContext({\n      viewTool: null,\n      sourceHydrationPending: true,\n      workHydrationFailed: true,\n      workHydrationError: \"공동 문서를 불러오지 못했습니다.\",\n      workId: \"private-draft\",\n      onRetrySourceHydration: retry,\n    });\n    render(<>{renderStudioCanvasStickyBanners(context)}</>);\n    expect(screen.getByText(\"원고를 열지 못했어요\")).toBeTruthy();\n    expect(screen.getByText(/빈 캔버스로 덮어쓰지 않도록/u)).toBeTruthy();\n    expect(screen.getByRole(\"alert\").textContent).toBe(context.workHydrationError);\n    fireEvent.click(screen.getByRole(\"button\", { name: \"다시 불러오기\" }));\n    expect(retry).toHaveBeenCalledOnce();\n    expect(context.navigate).not.toHaveBeenCalled();\n    expect(context.commitPages).not.toHaveBeenCalled();\n    expect(context.setCurrentPageId).not.toHaveBeenCalled();\n  });\n\n  it(\"does not expose retry during loading and keeps unsupported formats in their own editor\", () => {\n    const retry = vi.fn();\n    const context = createContext({ viewTool: null, sourceHydrationPending: true, onRetrySourceHydration: retry });\n    const { rerender } = render(<>{renderStudioCanvasStickyBanners(context)}</>);\n    expect(screen.queryByRole(\"button\", { name: \"다시 불러오기\" })).toBeNull();\n    rerender(<>{renderStudioCanvasStickyBanners({\n      ...context, workHydrationFailed: true, workHydrationUnsupportedFormat: true, workId: \"work/one\",\n    })}</>);\n    fireEvent.click(screen.getByRole(\"button\", { name: \"업로드 편집기로 이동\" }));\n    expect(context.navigate).toHaveBeenCalledWith(\"/studio?mode=upload&id=work%2Fone\");\n    expect(retry).not.toHaveBeenCalled();\n  });\n});\n"]]],
["apps/web/src/domains/creator/canvas/StudioCanvasViewport.tsx","34d52ee48b184d0aa3152767efbfef7ab18260b1a8b15761897620782c33bfbc","2a56de67d94e953d07f621c55fe67b5bde134121b961b0d861c5078f17cfca8a",[[121,121,"    workHydrationError,\n    onRetrySourceHydration,\n"],[385,385,"    workHydrationError,\n    onRetrySourceHydration,\n"]]],
["apps/web/src/domains/creator/canvas/StudioCanvasViewportTypes.ts","67ce3f71415b9b8b0278c6ee5e9db5aeabae68c07654e16c46b92f6f512107b0","15012b23de195ca254dfb851f2b47fd457965d63c23bf9ed8cb5cc2c37bd4dcb",[[550,550,"  workHydrationError?: string | null;\n  onRetrySourceHydration?: () => void;\n"]]],
["apps/web/src/domains/creator/studio-autosave-snapshot-fence.test.ts","e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855","de5e4c53a462a27b6f49d7b2ac9f82647a3260f88264d8aa60c8232a6900661b",[[0,0,"import { readFileSync } from \"node:fs\";\n\nimport { describe, expect, it, vi } from \"vitest\";\n\nimport { createStudioAutosaveSnapshotFence } from \"./studio-autosave-snapshot-fence\";\n\ndescribe(\"autosave captured snapshot fence\", () => {\n  it(\"rejects a second deferred stroke even when the React history generation did not change\", () => {\n    const session = {};\n    let current = { generation: 1, pendingFingerprint: \"p:first\", session };\n    const isCurrent = createStudioAutosaveSnapshotFence(current, () => current);\n    expect(isCurrent()).toBe(true);\n    current = { ...current, pendingFingerprint: \"p:first,second\" };\n    expect(isCurrent()).toBe(false);\n  });\n\n  it(\"rejects generation changes and a new document session\", () => {\n    const scheduled = { generation: 4, pendingFingerprint: \"\", session: {} };\n    let current = scheduled;\n    const isCurrent = createStudioAutosaveSnapshotFence(scheduled, () => current);\n    current = { ...scheduled, generation: 5 };\n    expect(isCurrent()).toBe(false);\n    current = { ...scheduled, session: {} };\n    expect(isCurrent()).toBe(false);\n  });\n\n  it(\"does not enqueue an obsolete write after asynchronous store preparation\", async () => {\n    const session = {};\n    let current = { generation: 2, pendingFingerprint: \"p:one\", session };\n    const isCurrent = createStudioAutosaveSnapshotFence(current, () => current);\n    let ready!: () => void;\n    const prepared = new Promise<void>((resolve) => { ready = resolve; });\n    const write = vi.fn();\n    const pending = (async () => {\n      await prepared;\n      if (isCurrent()) write();\n    })();\n    current = { ...current, pendingFingerprint: \"p:one,two\" };\n    ready();\n    await pending;\n    expect(write).not.toHaveBeenCalled();\n  });\n\n  it(\"wires the fence before the host's debounced write and tombstone paths\", () => {\n    const host = readFileSync(new URL(\"./StudioCuttoonEditorHost.tsx\", import.meta.url), \"utf8\");\n    const start = host.indexOf(\"const scheduledAutosaveSession =\");\n    const end = host.indexOf(\"// 서버 자동저장\", start);\n    const autosave = host.slice(start, end);\n    expect(start).toBeGreaterThan(-1);\n    expect(autosave).toContain(\"createStudioAutosaveSnapshotFence\");\n    expect(autosave).toContain(\"studioPendingStrokeFingerprint(pendingStrokeCommitsRef.current)\");\n    expect(autosave).toMatch(/if \\(!canPublishSnapshot\\(\\)\\) return;\\s+const receipt = await persistStudioAutosaveWithOpfsPrimary/u);\n    expect(autosave.indexOf(\"!canPublishSnapshot()\", autosave.indexOf(\"Promise.all([\")))\n      .toBeLessThan(autosave.indexOf(\"session.clear(savedAt)\"));\n  });\n});\n"]]],
["apps/web/src/domains/creator/studio-autosave-snapshot-fence.ts","e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855","b849a74195c0daf9ff395784601d3cbb434b57c4e82d0c83c7bb523741deff0a",[[0,0,"export interface StudioAutosaveSnapshotFrontier {\n  readonly generation: number;\n  readonly pendingFingerprint: string;\n  readonly session: unknown;\n}\n\n/** A debounce or async preparation may outlive the snapshot it captured. Never publish that\n * snapshot over a newer pointer-up checkpoint, or acknowledge it in a different document. */\nexport function createStudioAutosaveSnapshotFence(\n  scheduled: StudioAutosaveSnapshotFrontier,\n  readCurrent: () => StudioAutosaveSnapshotFrontier,\n): () => boolean {\n  return () => {\n    const current = readCurrent();\n    return scheduled.generation === current.generation\n      && scheduled.pendingFingerprint === current.pendingFingerprint\n      && scheduled.session === current.session;\n  };\n}\n"]]],
["apps/web/src/domains/creator/studio-cuttoon-editor/StudioCuttoonEditorCanvasColumn.tsx","1397efbe1fb6fc615ed928eeb174061b7033ae699e186fe9e3c6c773e8a0a78a","e562c99d000b2051501878bd4cb07a40a3aa32b8626ad70a80ea0a35f150f6a1",[[342,342,"    workHydrationError,\n    onRetrySourceHydration,\n"],[703,703,"        workHydrationError={workHydrationError}\n        onRetrySourceHydration={onRetrySourceHydration}\n"]]],
["apps/web/src/domains/creator/studio-cuttoon-editor/StudioCuttoonEditorViewSessionRest.ts","9accf9f8f1978e4b5b03b239427e42e7f6d585c3209a65f152c5376a8cdc6716","c2f73d03f71221bed4186e7f38fe448f353569b3e851e563bf8f8b74876b64e5",[[532,532,"  workHydrationError?: string | null;\n  onRetrySourceHydration?: () => void;\n"]]],
["apps/web/src/domains/creator/studio-release-schedule-empty-recovery.test.ts","e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855","d36b039e857b1c955992e2f34c729d3671bac99c126af282e9e0f524ae81a9c9",[[0,0,"import { describe, expect, it, vi } from \"vitest\";\n\nimport { normalizeStudioReleaseScheduleDeferred } from \"./studio-release-schedule-loader\";\n\nconst { loadPlanner } = vi.hoisted(() => ({ loadPlanner: vi.fn() }));\nvi.mock(\"./studio-release-schedule\", () => {\n  loadPlanner();\n  throw new Error(\"planner chunk is unavailable\");\n});\n\ndescribe(\"optional schedule failure isolation\", () => {\n  it(\"opens a manuscript with no schedule even when the planner chunk cannot load\", async () => {\n    await expect(normalizeStudioReleaseScheduleDeferred(undefined)).resolves.toEqual({ version: 1, items: [] });\n    await expect(normalizeStudioReleaseScheduleDeferred({ version: 1, items: [] })).resolves.toEqual({ version: 1, items: [] });\n    expect(loadPlanner).not.toHaveBeenCalled();\n  });\n\n  it(\"does not silently replace a nonempty schedule after an import failure\", async () => {\n    await expect(normalizeStudioReleaseScheduleDeferred({ version: 1, items: [{ id: \"keep-me\" }] })).rejects.toThrow();\n    expect(loadPlanner).toHaveBeenCalled();\n  });\n});\n"]]],
["apps/web/src/domains/creator/studio-release-schedule-loader.test.ts","489c14fc4883c35b0ec84b7cd3f5f5ad80618ba1768fb717af6ab4f3da53af90","f12fb3cf24233ce59c8402b1fcca4f8d3f8e542a518882952b1959b1f87f0823",[[8,8,"  isEmptyStudioReleaseScheduleSnapshot,\n  normalizeStudioReleaseScheduleDeferred,\n"],[57,57,"  it(\"recognizes only absent or exact empty optional schedules\", async () => {\n    for (const value of [undefined, null, { version: 1, items: [] }]) {\n      await expect(normalizeStudioReleaseScheduleDeferred(value)).resolves.toEqual({ version: 1, items: [] });\n    }\n    for (const value of [[], {}, { version: 2, items: [] }, { version: 1, items: [], entries: [{}] }, { version: 1, items: [{}] }]) {\n      expect(isEmptyStudioReleaseScheduleSnapshot(value)).toBe(false);\n    }\n  });\n\n"]]],
["apps/web/src/domains/creator/studio-release-schedule-loader.ts","376557a1bac4218a8fca333f268bb0056e6e96b53b92021f89739e7ebdd2eae4","fda2908d82363f28965f9c2abe189093b6450a86bcac93185196072369de3b0b",[[34,34,"\n/** Only the exact empty snapshot can avoid the planner; unknown/non-empty data must be validated. */\nexport function isEmptyStudioReleaseScheduleSnapshot(value: unknown): boolean {\n  if (typeof value !== \"object\" || value === null || Array.isArray(value)) return false;\n  const candidate = value as Record<string, unknown>;\n  const keys = Object.keys(candidate);\n  return keys.length === 2\n    && keys.includes(\"version\")\n    && keys.includes(\"items\")\n    && candidate.version === 1\n    && Array.isArray(candidate.items)\n    && candidate.items.length === 0;\n}\n\nexport async function normalizeStudioReleaseScheduleDeferred(\n  value: unknown,\n): Promise<StudioReleaseSchedule> {\n  if (value == null || isEmptyStudioReleaseScheduleSnapshot(value)) {\n    return createEmptyStudioReleaseScheduleSnapshot();\n  }\n  const { normalizeStudioReleaseSchedule } = await loadStudioReleaseScheduleRuntime();\n  return normalizeStudioReleaseSchedule(value);\n}\n"]]],
["apps/web/src/domains/creator/studio-shared-document-client.test.ts","a366589510215d4f933248641e6763d5802d3a7b78fbc6edf511d6d447531207","f0731e872cac140f79af09234ead1a171b39677d7b19e24740f06fc3f4463d74",[[398,399,"      expect.objectContaining({\n        timeout: 15_000,\n        retry: expect.objectContaining({ limit: 2, methods: [\"get\"] }),\n      })\n"],[400,400,"    const requestSignal = apiGet.mock.calls[0]?.[1]?.signal as AbortSignal;\n    expect(requestSignal.aborted).toBe(false);\n    controller.abort();\n    expect(requestSignal.aborted).toBe(true);\n"]]],
["apps/web/src/domains/creator/studio-shared-document-client.ts","34d44b46f8661577bfd06276543e7b7bfc269fa7ca9d10906e3d593bba4f5d8b","7146fbd9a1b34af0350316df3d80a3813c103b1ba13d79e3395c04b7f494044a",[[4,4,"import { creatorWorkReadOptions } from \"@/infrastructure/creator-work-read-options\";\n"],[571,572,"    payload = await api.get<unknown>(sharedDocumentPath(workId), creatorWorkReadOptions(signal));\n"]]],
["apps/web/src/domains/creator/studio-source-hydration-recovery-boundary.test.ts","e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855","90f08004ad53c5dade0c233acb007485ee46a530d358668cdb6734dd5ead2108",[[0,0,"import { readFileSync } from \"node:fs\";\n\nimport { describe, expect, it } from \"vitest\";\n\ndescribe(\"source hydration recovery ownership\", () => {\n  const host = readFileSync(new URL(\"./StudioCuttoonEditorHost.tsx\", import.meta.url), \"utf8\");\n  const start = host.indexOf(\"const [sourceHydrationAttempt,\");\n  const end = host.indexOf(\"sourceHydrationAttempt,\", start + 60);\n  const hydration = host.slice(start, end + 200);\n\n  it(\"does not classify an unresolved auth session as an anonymous source request\", () => {\n    expect(start).toBeGreaterThan(-1);\n    const gate = hydration.indexOf(\"if (!studioAuthReady) return;\");\n    expect(gate).toBeGreaterThan(-1);\n    expect(gate).toBeLessThan(hydration.indexOf(\"async function loadStudioWork\"));\n    expect(hydration).toContain(\"studioAuthReady,\");\n  });\n\n  it(\"prepares optional schedule data before changing canvas state and rejects stale responses\", () => {\n    expect(hydration).toContain(\"normalizeStudioReleaseScheduleDeferred(doc?.releaseSchedule)\");\n    expect(hydration).not.toContain(\"loadStudioReleaseScheduleRuntime()\");\n    expect(hydration).toContain(\"if (!alive || controller.signal.aborted) return;\");\n    expect(hydration).toContain(\"controller.abort()\");\n    expect(hydration).toContain(\"setWorkHydrationFailed(true)\");\n    expect(hydration).toContain(\"setWorkHydrationError(message)\");\n  });\n\n  it(\"passes an in-place retry action all the way to the locked canvas\", () => {\n    expect(host).toContain(\"onRetrySourceHydration={retrySourceHydration}\");\n    const viewport = readFileSync(new URL(\"./canvas/StudioCanvasViewport.tsx\", import.meta.url), \"utf8\");\n    expect(viewport.match(/onRetrySourceHydration/gu)?.length).toBeGreaterThanOrEqual(2);\n    const column = readFileSync(new URL(\"./studio-cuttoon-editor/StudioCuttoonEditorCanvasColumn.tsx\", import.meta.url), \"utf8\");\n    expect(column).toContain(\"onRetrySourceHydration={onRetrySourceHydration}\");\n  });\n});\n"]]],
["apps/web/src/infrastructure/creator-client.ts","a8047f10475b2ea0ee3e2e83ae8bc261d39cf8160e06e803d032e63f894d5291","6a4fdbd9ce67e3cb789733675cc2d8f3b619946587946b9eb9236deef0ea9a43",[[0,0,"import { creatorWorkReadOptions } from \"./creator-work-read-options\";\n"],[429,430,"    () => api.get<WorkDetail>(`${BASE}/works/${encodeURIComponent(id)}`, creatorWorkReadOptions(signal)),\n"]]],
["apps/web/src/infrastructure/creator-work-read-options.test.ts","e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855","e09e0a177f7c6e98c35b0e715ce7d1e2cb7e372f1a47b680d809d31300261722",[[0,0,"import ky from \"ky\";\nimport { afterEach, describe, expect, it, vi } from \"vitest\";\n\nimport { creatorWorkReadOptions } from \"./creator-work-read-options\";\n\nconst url = \"https://studio.test/api/creator/works/one\";\nconst jsonResponse = () => new Response('{\"title\":\"retained manuscript\"}', {\n  headers: { \"Content-Type\": \"application/json\" },\n});\n\nafterEach(() => vi.restoreAllMocks());\n\nfunction readOptions(signal?: AbortSignal) {\n  const options = creatorWorkReadOptions(signal);\n  // Preserve the production status/method/attempt policy, but avoid waiting between test attempts.\n  return { ...options, retry: { ...options.retry as object, delay: () => 0, jitter: false } };\n}\n\ndescribe(\"bounded manuscript GET recovery\", () => {\n  it(\"recovers a transient 503 and keeps the original snapshot\", async () => {\n    const fetch = vi.fn().mockResolvedValueOnce(new Response(\"busy\", { status: 503 }))\n      .mockResolvedValueOnce(jsonResponse());\n    await expect(ky.get(url, { ...readOptions(), fetch }).json()).resolves.toEqual({\n      title: \"retained manuscript\",\n    });\n    expect(fetch).toHaveBeenCalledTimes(2);\n  });\n\n  it(\"recovers a network failure without enabling mutation retries\", async () => {\n    const fetch = vi.fn().mockRejectedValueOnce(new TypeError(\"Failed to fetch\"))\n      .mockResolvedValueOnce(jsonResponse());\n    await expect(ky.get(url, { ...readOptions(), fetch }).json()).resolves.toHaveProperty(\"title\");\n    expect(fetch).toHaveBeenCalledTimes(2);\n    fetch.mockReset().mockImplementation(async () => new Response(\"busy\", { status: 503 }));\n    await expect(ky.patch(url, { ...readOptions(), fetch })).rejects.toHaveProperty(\"response.status\", 503);\n    expect(fetch).toHaveBeenCalledTimes(1);\n  });\n\n  it.each([400, 401, 403, 404, 409, 422])(\"does not retry HTTP %i\", async (status) => {\n    const fetch = vi.fn(async () => new Response(\"denied\", { status }));\n    await expect(ky.get(url, { ...readOptions(), fetch })).rejects.toHaveProperty(\"response.status\", status);\n    expect(fetch).toHaveBeenCalledTimes(1);\n  });\n\n  it(\"exhausts at three attempts rather than keeping the editor spinning forever\", async () => {\n    const fetch = vi.fn(async () => new Response(\"busy\", { status: 502 }));\n    await expect(ky.get(url, { ...readOptions(), fetch })).rejects.toHaveProperty(\"response.status\", 502);\n    expect(fetch).toHaveBeenCalledTimes(3);\n  });\n\n  it(\"retries response-header timeouts, with finite per-attempt and total budgets\", async () => {\n    const options = creatorWorkReadOptions();\n    expect(options.timeout).toBe(15_000);\n    expect(options.retry).toMatchObject({ limit: 2, retryOnTimeout: true });\n    const fetch = vi.fn(() => new Promise<Response>(() => {}));\n    await expect(ky.get(url, { ...readOptions(), timeout: 5, fetch })).rejects.toHaveProperty(\"name\", \"TimeoutError\");\n    expect(fetch).toHaveBeenCalledTimes(3);\n  });\n\n  it(\"propagates route cancellation through the combined deadline signal\", async () => {\n    const controller = new AbortController();\n    const options = creatorWorkReadOptions(controller.signal);\n    expect(options.signal?.aborted).toBe(false);\n    controller.abort();\n    expect(options.signal?.aborted).toBe(true);\n    const fetch = vi.fn(async (request: Request) => {\n      request.signal.throwIfAborted();\n      return jsonResponse();\n    });\n    await expect(ky.get(url, { ...options, fetch })).rejects.toHaveProperty(\"name\", \"AbortError\");\n    expect(fetch).toHaveBeenCalledTimes(1);\n    expect(fetch.mock.calls[0]?.[0].signal.aborted).toBe(true);\n  });\n\n  it(\"bounds even the body read and preserves the deadline when a caller also supplies a signal\", () => {\n    const deadline = new AbortController();\n    const timeout = vi.spyOn(AbortSignal, \"timeout\").mockReturnValue(deadline.signal);\n    const caller = new AbortController();\n    const options = creatorWorkReadOptions(caller.signal);\n    expect(timeout).toHaveBeenCalledWith(45_000);\n    deadline.abort(new DOMException(\"Read deadline exceeded\", \"TimeoutError\"));\n    expect(options.signal?.aborted).toBe(true);\n    expect(options.signal?.reason.name).toBe(\"TimeoutError\");\n    expect(caller.signal.aborted).toBe(false);\n  });\n\n  it(\"does not reinterpret malformed JSON as an empty manuscript or retry it\", async () => {\n    const fetch = vi.fn(async () => new Response(\"not JSON\"));\n    await expect(ky.get(url, { ...readOptions(), fetch }).json()).rejects.toBeInstanceOf(SyntaxError);\n    expect(fetch).toHaveBeenCalledTimes(1);\n  });\n});\n"]]],
["apps/web/src/infrastructure/creator-work-read-options.ts","e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855","92bf0a25c3ef1260e43deb7cab98b14e9731689fe5b490ec399a19b35c435a94",[[0,0,"import type { ApiOptions } from \"./api\";\n\n/** Only idempotent manuscript GETs opt in; publishing and save mutations never retry. */\nexport function creatorWorkReadOptions(signal?: AbortSignal): ApiOptions {\n  // This signal also bounds response.text(), which runs after the shared client's header timeout.\n  const deadline = AbortSignal.timeout(45_000);\n  return {\n    signal: signal ? AbortSignal.any([signal, deadline]) : deadline,\n    timeout: 15_000,\n    retry: {\n      limit: 2,\n      methods: [\"get\"],\n      statusCodes: [408, 429, 500, 502, 503, 504],\n      retryOnTimeout: true,\n      maxRetryAfter: 3_000,\n      backoffLimit: 1_500,\n      jitter: true,\n    },\n  };\n}\n"]]],
["scripts/verify-studio-brushes.mts","0f48ee8d98c547443c4e98243473c9170c61a7330eb3add6636b887719993afe","633b6f61187cf3d3daf35dc29e69987ff4cd19bdaae5749b7422c002647adeb4",[[3849,3850,"    await page.locator('[data-studio-menu-item-id=\"correct-current-stroke\"]').click();\n"]]]]
''')
for name, before, after, edits in updates:
    path = Path(name)
    assert name.startswith(("apps/web/src/", "scripts/")), name
    raw = path.read_bytes() if path.exists() else b""
    assert hashlib.sha256(raw).hexdigest() == before, f"Stale source: {name}"
    lines = raw.decode("utf-8").splitlines(keepends=True)
    for start, end, replacement in reversed(edits):
        lines[start:end] = [replacement]
    updated = "".join(lines).encode("utf-8")
    assert hashlib.sha256(updated).hexdigest() == after, f"Patch output mismatch: {name}"
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_bytes(updated)
print(f"Applied {len(updates)} exact reviewed source changes")
